import React,{propTypes} from 'react' ;
import ProgressBar from  '../common/ProgressBar/ProgressBar'

const Home = (props)=>(
	<div>
		<h2>这里是主页，暂时没有内容！</h2>
		<ProgressBar/>
	</div>
)

export default Home ;